const config = {
    port: 3302,

    database: {
        DATABASE: 'case',
        USERNAME: 'root',
        PASSWORD: 'Changeme_123',
        PORT: '3306',
        HOST: 'localhost'
    }
};

module.exports = config;